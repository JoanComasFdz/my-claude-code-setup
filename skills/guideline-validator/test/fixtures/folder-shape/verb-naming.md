# Verb naming
Every function or method name must contain a verb describing what it does.
A bare-noun name (e.g. `Fingerprint`, `Sides`) denotes a thing, not an action, so it belongs to data.
Examples: `ReadBusinessDate`, `ResolveAccount`, `IsWorkbook`.
